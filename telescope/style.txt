/* General Reset */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Arial', sans-serif;
    background-color: #0a0e17;
    color: #e0e0e0;
    line-height: 1.6;
}

/* Header Section with Blurred Background */
/* Header Section with Parallax Scrolling Background */
/* Header Section with Parallax and Blurred Background Image */
header {
    position: relative;
    width: 100%;
    height: 80vh; /* Adjust height as necessary */
    overflow: hidden;
}

header::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url('ALL.jpeg'); /* Your background image */
    background-size: cover;
    background-position: center;
    background-attachment: fixed; /* Parallax scrolling */
    filter: blur(3px); /* Apply the blur only to the background */
    z-index: -1; /* Make sure it stays behind the text */
}

.header-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: rgba(0, 0, 0, 0.6); /* Dark overlay for text readability */
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    color: #fff;
}

.header-overlay h1 {
    font-size: 4rem;
    margin-bottom: 10px;
    letter-spacing: 2px;
    z-index: 1;
}

.header-overlay p {
    font-size: 1.5rem;
    opacity: 0.9;
    z-index: 1;
}



/* Main content */
main {
    padding: 40px 20px;
    max-width: 1200px;
    margin: 0 auto;
}

.component {
    display: flex;
    background-color: #1b2a41;
    margin-bottom: 30px;
    border-radius: 10px;
    overflow: hidden;
    box-shadow: 0px 10px 20px rgba(0, 0, 0, 0.3);
    transition: transform 0.3s ease-in-out;
}

.component:hover {
    transform: translateY(-10px);
}

.image-box {
    flex: 1;
    background-color: #243b55;
    padding: 20px;
}

.image-box img {
    max-width: 100%;
    border-radius: 10px;
}

.text-box {
    flex: 2;
    padding: 40px;
    display: flex;
    flex-direction: column;
    justify-content: center;
}

.text-box h2 {
    font-size: 2rem;
    margin-bottom: 15px;
    color: #00aaff;
}

.text-box p {
    font-size: 1.2rem;
    margin-bottom: 10px;
}

.text-box strong {
    color: #ffcc00;
}

footer {
    text-align: center;
    padding: 20px;
    background-color: #141e30;
    color: #fff;
    font-size: 1rem;
    letter-spacing: 1px;
}
